# Audit — Phase 2L.32A: Final Repository Cleanup + Pre-Commit Readiness

**Date:** 2026-06-23  **Branch:** `main`  **Status:** uncommitted (ready to commit)

## Cleanup actions taken

- Removed all **project** Python caches (`__pycache__/` under `src/`, `scripts/`, `tests/`,
  `src/tool_solvers/`, `src/formula_cards/`) and `.pytest_cache/` (1361 cache dirs existed
  including `.venv`; only project caches were removed — `.venv/` was left untouched). These
  are gitignored, so this is pure disk hygiene.
- Removed two clearly-disposable smoke output dirs: `scratch/final_default_short_smoke/` and
  `scratch/final_package_smoke/` (regenerable smoke artifacts).
- **Fixed `.dockerignore`** to exclude `.venv/`, `venv/`, `env/`, `data/`, `output/` — the
  build context was **4.14 GB** (it was copying the 4 GB host `.venv` with torch) and the
  first `docker build` failed with "no space left on device". After the fix the context is
  tiny and the build succeeds.
- **Added `.gitignore` exceptions** so the required final artifacts are trackable (see
  packaging note below).

## Files removed

- Project `__pycache__/` dirs + `.pytest_cache/` (gitignored; regenerated by pytest).
- `scratch/final_default_short_smoke/`, `scratch/final_package_smoke/`.

## Files intentionally kept

- `outputs/pred_v11_independent_rerun1.csv` (winning 78.4), `outputs/pred_v10_full_production_user_run.csv`
  (fallback), and the other `outputs/` CSVs.
- All audit docs under `docs/`, all final configs/scripts/tests.
- Run evidence under `scratch/full_v11_independent_rerun1/` (the repaired decisions +
  candidates that produced the winning submission) and other phase scratch dirs.

## Final default confirmation

Production default unchanged: **independent v11**, `outputs/pred_v11_independent_rerun1.csv`,
public **78.4**, offline `frozen_csv` mode; v10 is fallback only. Config
`configs/production_v11_independent.json`: `default_mode=frozen_csv`,
`current_best_csv=outputs/pred_v11_independent_rerun1.csv`.

## Exact short command confirmation

```bash
python scripts/final_infer.py --input public-test_1780368312.json --output pred.csv
```
Works with **no `--mode`, no `--allow-pred-csv`, no API key** (verified by the smoke run).

## Final smoke result (Part C)

```
final_infer.py --output scratch/final_precommit_smoke/pred.csv
  -> FINAL INFER COMPLETE  mode: frozen_csv  questions: 463
     md5: 69f4e7c990e8c612e7bee53084d13b4d  elapsed_seconds: 0.02  status: PASS
validate_submission.py -> RESULT: PASS — submission is valid.
```

## md5 match result

```
best  (outputs/pred_v11_independent_rerun1.csv)     : 69f4e7c990e8c612e7bee53084d13b4d
smoke (scratch/final_precommit_smoke/pred.csv)      : 69f4e7c990e8c612e7bee53084d13b4d
same: True
```

## validate_submission result

`RESULT: PASS — submission is valid.` (both the host smoke and the Docker output).

## Docker build/run result (Part D)

- `docker build -t fastmcq-final .` — **SUCCESS** after the `.dockerignore` `.venv` fix
  (first attempt failed on a 4.14 GB context / no space; second attempt built cleanly).
- `docker run` with mounted `scratch/docker_smoke/{data,output}` — **SUCCESS**: the default
  frozen entrypoint wrote `/output/pred.csv` and self-validated `PASS`. Container output md5
  = **`69f4e7c990e8c612e7bee53084d13b4d`** (== winning CSV); host validator also `PASS`.
- No API key was provided or required.

## Production candidate audit summary (Part E)

`scripts/audit_production_candidate.py` →
`candidate valid=True, qid_set_valid=True, changed_vs_v10=48, beats_v10=True,
md5_matches_manifest=True` → **recommendation: `freeze_as_default`**
(report under `scratch/production_candidate_audit_final/`).

## Tests run and results

- `compileall -q src scripts tests`: **OK**
- `pytest -q`: **605 passed**.
- `scripts/audit_model_policy.py`: **PASS — only competition-allowed models referenced.**

## Ignore-rule check result (Part B)

Before: `.gitignore` line `outputs/*` (only `.gitkeep` excepted) **ignored the winning CSV**
— `git check-ignore` matched `outputs/*` for both required CSVs and `git ls-files outputs/`
showed only `.gitkeep`. The image was unaffected (Docker `COPY .` reads the filesystem), but
a `git` clone/package would have been missing the winning CSV.

After: added exceptions
```
!outputs/pred_v11_independent_rerun1.csv
!outputs/pred_v10_full_production_user_run.csv
```
Now both required CSVs appear in `git status` as `??` (trackable); `outputs/pred.csv`
(runtime export) and `outputs/pred_v11_full_adaptive_test.csv` / `pred_v8_*` (experimental)
remain ignored. `.dockerignore` keeps the winning CSV + v10 fallback in the image and
excludes secrets/`.env`/`.git`/`scratch`/`.venv`/caches/notebooks/weights.

## Whether the winning CSV is tracked / ignored / packaged

**Decision: package it via git.** The winning CSV is required to build & run the final
package offline, so it should not stay git-ignored. This phase un-ignored it (and the v10
fallback) via explicit `.gitignore` exceptions; they are now staged-eligible and will be
committed by the recommended command below. (Docker already included them from the
filesystem; this makes a `git clone` self-sufficient too.)

## Confirmations

- **No OpenRouter/API call**; no inference; no full run (frozen_csv + Docker frozen export
  only).
- **No best outputs overwritten** — `outputs/pred_v11_independent_rerun1.csv` md5 still
  `69f4e7c990e8c612e7bee53084d13b4d`; v10, v8, and the existing `outputs/pred.csv` untouched.
  Smoke + Docker wrote only under `scratch/`.
- Default is still the winning v11 frozen CSV; v10 not made default.
- No qid hardcoding; no answer tables / ground truth; external 3-LLM sheet not used.
- All LLM/rerank paths obey `src/model_policy.py` (policy audit PASS); no disallowed model.
- Nothing committed.

## Final `git status --short`

```
 M .gitignore          (added outputs exceptions for required CSVs)
 M Dockerfile          (2L.31A: v11 frozen entrypoint)
 M README.md           (added Final-submission pointer to the v11 78.4 command)
 M scripts/run_production_pipeline.py   (pre-existing, earlier phases)
 M src/formula_bank_solver.py           (pre-existing, earlier phases)
 M src/knowledge_cards.py               (pre-existing, earlier phases)
 M src/openrouter_client.py             (pre-existing, earlier phases)
 M tests/test_candidate_lab.py          (pre-existing, earlier phases)
 M tests/test_formula_bank_solver.py    (pre-existing, earlier phases)
?? .dockerignore, DOCKER_SUBMISSION.md, FINAL_RUN.md
?? configs/production_v11_independent.json
?? experiments/best_candidate_manifest.json
?? outputs/pred_v11_independent_rerun1.csv   (winning 78.4 — now trackable)
?? outputs/pred_v10_full_production_user_run.csv (fallback — now trackable)
?? scripts/*.py (final_infer, run_full_v11_independent_submission, repair_v11_*, audit_*, …)
?? src/*.py (independent_answer_selector, calculation_first_planner, answer_factory, …)
?? tests/test_*.py (final_package, btc_short, independent_v11, repair_v11, v11_hardening, …)
?? docs/AUDIT_PHASE_2L25..2L32A_*.md
```
(Full untracked list is large — the v11 pipeline + audits from phases 2L.25–2L.32A were
never committed. `.venv/` and `scratch/` remain ignored.)

## Recommended commit command

Review, then stage everything (the `.gitignore` exceptions make the two required CSVs
stage-able) and commit on a branch (current branch is `main`):

```bash
git checkout -b v11-independent-freeze
git add -A
git status            # confirm the winning CSV + v10 fallback are staged, pred.csv is NOT
git commit -m "Freeze winning independent v11 (78.4) as production default + final package

- final_infer.py frozen_csv default (offline, no API key), prints elapsed + validates
- Docker v11 frozen entrypoint; .dockerignore excludes .venv/secrets/scratch
- manifest + production config + FINAL_RUN/DOCKER_SUBMISSION docs
- track required final CSVs (winning v11 + v10 fallback)
- full v11 independent runner + repair + integrity audit; 605 tests passing"
```
(Do not commit until you have reviewed the staged set, especially the `outputs/` CSVs.)

# FASTMCQ dependencies (Phase 2L.47B — offline local-model submission).
#
# torch is NOT listed here: it is exact-pinned in the Dockerfile from the PyTorch CUDA 12.8 index
# (`pip install --index-url https://download.pytorch.org/whl/cu128 torch==2.7.1`) so it matches
# BTC's CUDA 12.8+ GPU environment. Everything below is pip-installable from the default index.

# --- Local open-weight model inference (final offline path) ---
transformers==5.12.1          # Qwen3 family support
accelerate==1.14.0            # device_map / efficient loading
safetensors==0.8.0            # weights format
huggingface_hub==1.21.0       # snapshot_download (model fetched at BUILD time)
sentencepiece==0.2.1          # tokenizer dependency for some models

# --- Core utilities ---
PyYAML==6.0.3                 # config loading

# --- Legacy / dev only (NOT used by the offline final path) ---
# httpx is the HTTP client for the older OpenRouter dynamic pipeline, reachable only via the
# explicit dev flag `--legacy-dynamic-full`. Kept so that path + its tests still import.
httpx==0.28.1

# --- Testing (not required at inference time) ---
pytest==9.1.0

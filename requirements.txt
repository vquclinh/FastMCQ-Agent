# Phase 1 dependencies — intentionally minimal.
# Core pipeline (data_io, labels, solver, postprocess) uses only the Python
# standard library. PyYAML is the single runtime dependency, for config loading.
PyYAML>=6.0

# Runtime HTTP client for the OpenRouter/selective API path (src/api/openrouter_client.py).
# Imported lazily and only used when running with an API key (e.g. the api-baked image or
# `-e OPENROUTER_API_KEY=...`); the offline no-api path never needs it.
httpx>=0.27

# Testing (optional; tests also run standalone without pytest).
pytest>=7.0
